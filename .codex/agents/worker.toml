name = "worker"
description = "Implement one bounded change using project patterns and required checks."
model = "gpt-5.6-terra"
reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement one bounded change, preserve existing project patterns, and run the assigned checks.
Stop when the assigned scope or risk boundary is exceeded.
Return these labeled fields:
status
files inspected
files changed
implementation or findings
checks run
assumptions
residual risks
recommended next action
"""
